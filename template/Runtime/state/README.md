# Run state

- `runs/` é **gitignored** (efêmero).
- `templates/` são versionados.
- Opcional: arquivo `CURRENT` com o run-id ativo (uma linha).

```text
Runtime/state/
├── README.md
├── CURRENT
├── templates/
└── runs/<run-id>/
```
